# 五项目适配状态

更新时间：2026-09-15

| 项目 | 启动适配 | 完成检测 | 失败检测 | 权限 | 真实队列验证 |
| --- | --- | --- | --- | --- | --- |
| BGI | `BetterGI.exe --startOneDragon` | `better-genshin-impact*.log` / `一条龙和配置组任务结束` | `一条龙在启动阶段被取消` | 普通权限 | 待验证 |
| MAA | `MAA.exe` | `debug/gui.log` / `任务已全部完成！` | 暂不设置，避免将可恢复的子任务错误误判为全局失败 | 普通权限 | 已验证完成、清理及切换至 MFA |
| ZOG | `OneDragon-Launcher.exe -o -c` | `.log/log.txt` / 一条龙成功且全部结束 | 一条龙执行失败 | 普通权限 | 启动和持续等待已验证；一次真实运行因游戏窗口中途退出而失败，待成功验证 |
| MFA | `MFAAvalonia.exe` | `logs/log-*.log` / `任务已全部完成！` | `停止前状态：FAILED` | 普通权限 | 已验证由 MAA 顺序启动并完成；推荐日志模式待单项复验 |
| M7A | `March7th Launcher.exe main -e` | `logs/*.log` / `游戏终止：StarRail` | 暂无足够稳定的全局失败标志 | 需要管理员权限 | 待在 UAC 确认后验证 |

## 当前结论

- 五个程序均可从本机 `D:\Tool` 目录识别。
- MAA → MFA 的真实顺序执行、5 秒间隔、完成和清理已经通过。
- ZOG 已确认采用“启动器快速退出、后台继续运行”的结构，不能使用主进程退出作为完成条件。
- M7A 的启动清单要求管理员权限，普通权限直接启动会被 Windows 拒绝。
- 尚不能宣称五项目全部适配完成；BGI、ZOG 成功流程、MFA 日志模式和 M7A 提权流程仍需逐项验证。

## 下一轮验证顺序

1. MFA 日志关键字模式单项验证。
2. BGI `--startOneDragon` 单项验证。
3. ZOG 在游戏运行稳定时复验成功流程。
4. M7A 由用户确认 UAC 后验证完整流程。
5. 五项目全部启用后的顺序队列验证。

