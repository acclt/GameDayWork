# 五项目进程退出适配状态

更新时间：2026-09-15

当前阶段只使用“指定进程退出”作为完成条件。任务进程无论正常结束、中断或崩溃，只要连续 3 秒确认已退出，调度器就执行清理并进入下一项。日志关键字能力仍保留，但不作为这五个项目推荐配置的默认检测方式。

| 项目 | 启动方式 | 当前监控进程 | 权限 | 验证状态 |
| --- | --- | --- | --- | --- |
| BGI | `BetterGI.exe --startOneDragon` | `BetterGI.exe` | 普通权限 | 待验证 |
| MAA | `MAA.exe` | `MAA.exe` | 普通权限 | 已验证退出、清理并切换至 MFA |
| ZOG | `OneDragon-Launcher.exe -o -c` | 本次启动的 `OneDragon-Launcher.exe` | 普通权限 | 已确认存在多层启动器，待完整队列复验 |
| MFA | `MFAAvalonia.exe` | `MFAAvalonia.exe` | 普通权限 | 已验证由 MAA 顺序启动并完成 |
| M7A | `March7th Launcher.exe main -e` | `March7th Assistant.exe` | 需要管理员权限 | 待在 UAC 确认后验证 |

## 当前规则

- 启动任务前记录进程快照，不接管已经存在的同名进程。
- 优先按照本次进程树、程序完整路径和安装目录识别关联进程。
- 只允许匹配本次任务启动后新出现的同名进程。
- 指定进程消失后连续观察 3 秒，避免启动器交接瞬间误判完成。
- ZOG 监控本次启动产生的全部同名启动器；M7A 监控实际执行任务的 Assistant，而不是外层 Launcher。
- 最大运行时间只作为卡死兜底，不参与正常完成判断。

## 下一轮验证顺序

1. 分别验证 BGI、ZOG、MFA 的任务进程退出后能进入下一项。
2. M7A 由用户确认 UAC 后验证实际任务进程识别。
3. 启用五项目，执行完整顺序队列验证。
