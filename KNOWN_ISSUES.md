# 已知问题

## GO-001：运行任务时状态卡仍显示“暂无任务运行”

- 状态：待修复
- 优先级：中
- 记录日期：2026-09-15
- 影响范围：主窗口“当前状态”卡片

### 复现方式

1. 启用任意有效任务并开始执行队列。
2. 等待任务进入 `RUNNING` 状态。
3. 查看主窗口右上方“当前状态”卡片。

### 当前表现

顶部状态和卡片标签显示“执行中”，下方详细信息也能正确显示当前任务、运行时间、阶段和主 PID；但卡片中央仍显示“暂无任务运行”以及“点击「开始执行」或等待定时任务”。

### 预期表现

存在活动会话时，卡片中央应显示当前任务名称和运行提示；仅在没有活动任务时显示空闲占位内容。

### 初步定位

`Views/MainWindow.xaml` 中的两段空闲提示为固定文本，没有绑定 `CurrentTaskName`，也没有根据 `CurrentSession` 或任务状态切换可见性。

### 验收条件

- 任务处于启动、运行、完成检测或清理阶段时，不显示“暂无任务运行”。
- 卡片中央展示的任务名称与下方“当前任务”字段一致。
- 队列空闲且没有活动会话时，恢复显示空闲占位内容。

