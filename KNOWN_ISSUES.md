# 已知问题

## GO-001：运行任务时状态卡仍显示“暂无任务运行”

- 状态：已修复
- 优先级：中
- 记录日期：2026-09-15
- 修复日期：2026-09-15
- 影响范围：主窗口“当前状态”卡片

### 复现方式

1. 启用任意有效任务并开始执行队列。
2. 等待任务进入 `RUNNING` 状态。
3. 查看主窗口右上方“当前状态”卡片。

### 当前表现

顶部状态和卡片标签显示“执行中”，下方详细信息也能正确显示当前任务、运行时间、阶段和主 PID；但卡片中央仍显示“暂无任务运行”以及“点击「开始执行」或等待定时任务”。

### 预期表现

存在活动会话时，卡片中央应显示当前任务名称和运行提示；仅在没有活动任务时显示空闲占位内容。

### 初步定位

`Views/MainWindow.xaml` 中的两段空闲提示为固定文本，没有绑定 `CurrentTaskName`，也没有根据 `CurrentSession` 或任务状态切换可见性。

### 验收条件

- 任务处于启动、运行、完成检测或清理阶段时，不显示“暂无任务运行”。
- 卡片中央展示的任务名称与下方“当前任务”字段一致。
- 队列空闲且没有活动会话时，恢复显示空闲占位内容。

### 修复说明

状态卡中央文字改为绑定当前活动会话；任务名称、阶段、PID 和进程数量统一使用活动状态判断，任务完成后恢复空闲显示。

## GO-002：窗口无法通过右上角 X 或任务栏菜单关闭

- 状态：已修复
- 优先级：高
- 记录日期：2026-09-15
- 修复日期：2026-09-15
- 影响范围：主窗口关闭流程

### 当前表现

点击窗口右上角 X，或从任务栏右键选择“关闭窗口”后，程序没有响应并继续驻留。

### 原因

关闭事件在界面线程同步等待异步配置保存；保存操作的后续步骤又等待返回界面线程，形成死锁。

### 修复方案

首次收到关闭请求时暂缓关闭，异步完成配置保存；保存完成后再次触发关闭，并释放调度器资源。保存失败不阻止程序退出。

### 验证结果

启动修复版真实窗口后发送标准窗口关闭请求，窗口接受请求并在 8 秒限制内自行退出，没有遗留进程。
